'use client'
import Image from 'next/image'
import { FormEvent, useState } from 'react'
import { useRouter } from 'next/navigation'
import { supabase } from '../../lib/supabase'
export default function LoginPage(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[message,setMessage]=useState('');const[loading,setLoading]=useState(false);const router=useRouter();async function signIn(e:FormEvent){e.preventDefault();setLoading(true);const{error}=await supabase.auth.signInWithPassword({email,password});setLoading(false);if(error){setMessage('Correo o contraseña incorrectos.');return}router.replace('/')}return <main className="newgen-login-bg grid min-h-screen place-items-center p-5"><span className="money-sticker">S/</span><span className="money-sticker">S/</span><span className="money-sticker">S/</span><span className="money-sticker">S/</span><form onSubmit={signIn} className="newgen-login-card w-full max-w-md overflow-hidden rounded-3xl bg-white/95 shadow-2xl shadow-emerald-950/20"><div className="flex flex-col items-center justify-center bg-[#2fac89] px-8 py-7 text-center text-white"><Image src="/brand/newgen-peru.png" alt="NG" width={520} height={260} priority className="h-32 w-60 object-contain"/><p className="-mt-5 text-lg font-black uppercase tracking-[.2em] text-white/95">Newgen Peru</p></div><div className="p-8"><h1 className="text-2xl font-black text-slate-900">Iniciar sesión</h1><p className="mt-1 text-sm text-slate-500">Ingresa con tu cuenta autorizada.</p><label className="mt-6 block text-sm font-bold">Correo<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" className="mt-2 w-full rounded-xl border border-slate-200 bg-white p-3 outline-none focus:border-blue-500"/></label><label className="mt-4 block text-sm font-bold">Contraseña<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" className="mt-2 w-full rounded-xl border border-slate-200 bg-white p-3 outline-none focus:border-blue-500"/></label><button disabled={loading} className="mt-6 w-full rounded-xl bg-blue-600 p-3 text-sm font-black text-white shadow-lg shadow-emerald-800/20 disabled:opacity-60">{loading?'Ingresando...':'Ingresar'}</button>{message&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-sm font-bold text-red-700">{message}</p>}</div></form></main>}
